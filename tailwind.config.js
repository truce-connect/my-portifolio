module.exports = {
  content: [
    './src/**/*.{html,js,jsx,ts,tsx}', // Ensure content sources are configured
    './public/index.html',
  ],
  darkMode: 'media', // or 'class', or remove entirely if not using dark mode
  theme: {
    extend: {},
  },
  plugins: [],
};
